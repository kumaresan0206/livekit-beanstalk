version: 0.2

phases:
  build:
    commands:
      - echo ">> Executing LiveKit Node.js WebRTC Unit Test Suite..."
      - node --test tests/configuration.test.js
      - echo ">> All Node.js Unit Tests Passed Successfully."
